import Navbar from '../../components/Navbar'; import PosterCard from '../../components/PosterCard'; import useLocalStorage from '../../hooks/useLocalStorage'; import { Link } from 'react-router-dom';
export default function MyList(){const [list,setList]=useLocalStorage('streamflix-my-list',[]);const remove=i=>setList(p=>p.filter(x=>x.id!==i.id));return <div className="min-h-screen bg-[#08090b] text-white"><Navbar/><main className="mx-auto max-w-[1600px] px-5 pb-20 pt-32 sm:px-8 lg:px-10"><p className="text-xs font-bold uppercase tracking-[.3em] text-red-500">Your library</p><h1 className="mt-2 text-5xl font-black tracking-tight">My List</h1>{list.length?<div className="mt-10 grid grid-cols-2 gap-x-3 gap-y-8 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-5 xl:grid-cols-6">{list.map(item=><PosterCard key={item.id} item={item} onToggleList={remove} inList/>)}</div>:<div className="mt-20 rounded-3xl border border-dashed border-white/10 bg-white/[.02] p-16 text-center"><h2 className="text-2xl font-bold">Your list is waiting</h2><p className="mx-auto mt-3 max-w-md text-sm leading-6 text-white/40">Save movies and series you want to watch later. Your list stays on this device.</p><Link to="/movies" className="mt-7 inline-flex rounded-xl bg-white px-5 py-3 text-sm font-bold text-black">Browse movies</Link></div>}</main></div>}
