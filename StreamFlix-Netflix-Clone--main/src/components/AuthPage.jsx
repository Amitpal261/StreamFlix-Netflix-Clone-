import { useState } from 'react';
import { Link, useNavigate, useSearchParams } from 'react-router-dom';
import { Eye, EyeOff, Loader2 } from 'lucide-react';
import { createUserWithEmailAndPassword, signInWithEmailAndPassword, updateProfile } from 'firebase/auth';
import { auth } from '../../utils/firebase';
export default function AuthPage({mode='login'}){const signup=mode==='signup';const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [name,setName]=useState('');const [show,setShow]=useState(false);const [error,setError]=useState('');const [busy,setBusy]=useState(false);const nav=useNavigate();const [params]=useSearchParams();const submit=async e=>{e.preventDefault();setError('');setBusy(true);try{if(signup){const cred=await createUserWithEmailAndPassword(auth,email,password);if(name.trim())await updateProfile(cred.user,{displayName:name.trim()})}else await signInWithEmailAndPassword(auth,email,password);nav(params.get('next')||'/home',{replace:true})}catch(err){setError(err.code?.replace('auth/','').replaceAll('-',' ')||'Authentication failed')}finally{setBusy(false)}};return <div className="relative grid min-h-screen place-items-center overflow-hidden bg-[#08090b] px-5 py-16"><div className="absolute inset-0 bg-[radial-gradient(circle_at_50%_20%,rgba(229,9,20,.14),transparent_38%)]"/><div className="relative w-full max-w-md"><Link to="/" className="mb-10 block text-center text-3xl font-black tracking-[-.08em] text-[#e50914]">STREAMFLIX</Link><div className="rounded-3xl border border-white/10 bg-white/[.045] p-7 shadow-2xl backdrop-blur-2xl sm:p-9"><p className="text-xs font-bold uppercase tracking-[.3em] text-red-500">Your entertainment, your space</p><h1 className="mt-3 text-3xl font-bold">{signup?'Create your account':'Welcome back'}</h1><p className="mt-2 text-sm text-white/40">{signup?'Start building your personal streaming library.':'Sign in to continue watching.'}</p><form onSubmit={submit} className="mt-7 space-y-4">{signup&&<input required value={name} onChange={e=>setName(e.target.value)} placeholder="Full name" className="h-12 w-full rounded-xl border border-white/10 bg-black/20 px-4 outline-none transition focus:border-red-500/60"/>}<input required type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email address" className="h-12 w-full rounded-xl border border-white/10 bg-black/20 px-4 outline-none transition focus:border-red-500/60"/><div className="relative"><input required minLength={6} type={show?'text':'password'} value={password} onChange={e=>setPassword(e.target.value)} placeholder="Password" className="h-12 w-full rounded-xl border border-white/10 bg-black/20 px-4 pr-12 outline-none transition focus:border-red-500/60"/><button type="button" onClick={()=>setShow(!show)} className="absolute right-3 top-1/2 -translate-y-1/2 text-white/40">{show?<EyeOff size={18}/>:<Eye size={18}/>}</button></div>{error&&<p className="rounded-xl border border-red-500/20 bg-red-500/10 px-4 py-3 text-sm text-red-300">{error}</p>}<button disabled={busy} className="flex h-12 w-full items-center justify-center gap-2 rounded-xl bg-[#e50914] font-bold transition hover:bg-[#f6121d] disabled:opacity-60">{busy&&<Loader2 className="animate-spin" size={18}/>} {signup?'Create account':'Sign in'}</button></form><p className="mt-6 text-center text-sm text-white/40">{signup?'Already have an account? ':'New to StreamFlix? '}<Link className="font-semibold text-white hover:text-red-400" to={signup?'/login':'/signup'}>{signup?'Sign in':'Create an account'}</Link></p></div></div></div>}
