import { motion } from 'framer-motion';
import { Info, Play, Plus, Check } from 'lucide-react';
import { Link } from 'react-router-dom';
import { backdropUrl } from '../services/tmdb';
export default function Hero({item,onPlay,onToggleList,inList}){if(!item)return <div className="h-[72vh] min-h-[560px] bg-gradient-to-b from-white/5 to-[#08090b]"/>;const title=item.title||item.name;const type=item.media_type|| (item.title?'movie':'tv');return <section className="relative min-h-[680px] overflow-hidden"><div className="absolute inset-0"><img src={backdropUrl(item.backdrop_path)} alt="" className="h-full w-full object-cover object-center"/><div className="absolute inset-0 bg-gradient-to-r from-[#08090b] via-[#08090b]/75 to-transparent"/><div className="absolute inset-0 bg-gradient-to-t from-[#08090b] via-transparent to-[#08090b]/20"/></div><div className="relative mx-auto flex min-h-[680px] max-w-[1600px] items-end px-5 pb-20 pt-32 sm:px-8 lg:px-10"><motion.div initial={{opacity:0,y:20}} animate={{opacity:1,y:0}} transition={{duration:.5}} className="max-w-2xl"><p className="mb-4 flex items-center gap-2 text-xs font-bold uppercase tracking-[.3em] text-red-500"><span className="h-px w-8 bg-red-500"/>Featured tonight</p><h1 className="text-5xl font-black tracking-[-.05em] sm:text-7xl lg:text-8xl">{title}</h1><div className="mt-5 flex flex-wrap items-center gap-3 text-sm text-white/70"><span className="font-semibold text-emerald-400">{Math.round((item.vote_average||0)*10)}% Match</span><span>{item.release_date?.slice(0,4)||item.first_air_date?.slice(0,4)}</span><span className="rounded border border-white/20 px-1.5 py-0.5 text-[10px]">HD</span><span>{item.media_type==='tv'?'Series':'Movie'}</span></div><p className="mt-5 line-clamp-3 max-w-xl text-sm leading-7 text-white/55 sm:text-base">{item.overview||'Discover your next favorite story on StreamFlix.'}</p><div className="mt-7 flex flex-wrap gap-3"><button onClick={()=>onPlay?.(item)} className="inline-flex items-center gap-2 rounded-lg bg-white px-6 py-3 text-sm font-bold text-black transition hover:bg-white/85"><Play size={17} fill="currentColor"/> Play</button><button onClick={()=>onToggleList?.(item)} className="inline-flex items-center gap-2 rounded-lg bg-white/10 px-5 py-3 text-sm font-semibold text-white backdrop-blur transition hover:bg-white/15">{inList?<Check size={17}/>:<Plus size={17}/>} {inList?'In My List':'My List'}</button><Link to={`/${type}/${item.id}`} className="inline-flex items-center gap-2 rounded-lg border border-white/10 bg-black/25 px-5 py-3 text-sm font-semibold text-white/80 backdrop-blur transition hover:bg-white/10 hover:text-white"><Info size={17}/> More Info</Link></div></motion.div></div></section>}
